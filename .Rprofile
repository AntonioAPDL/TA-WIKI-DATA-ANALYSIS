# Interactive sessions activate renv automatically. Command-line entry points
# activate it only after dispatching commands that do not need the R environment
# (help, privacy, and the opt-in bootstrap).
if (interactive()) source("renv/activate.R")
